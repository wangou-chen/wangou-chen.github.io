# 软件构造实验三：代码重构与可维护性设计心得
日期：2026-05-09

## 一、实验主要内容
本次实验聚焦代码坏味道识别、增量重构与架构优化：
1. 使用 radon 量化圈复杂度，定位上帝函数、过长方法；
2. 按单一职责原则拆分臃肿路由函数，业务逻辑下沉到服务层；
3. 抽离文件处理、联网搜索、知识库、图谱检索为独立服务；
4. 引入策略模式统一多信息增强通道，消除大量 if-else；
5. 在不改变原有业务功能的前提下，提升代码可维护性与可扩展性。

## 二、实验遇到的问题
1. 单个函数承担过多职责，代码冗长、嵌套深、难以维护；
2. 新增检索能力只能不断追加 if-else，扩展性极差；
3. 业务逻辑和接口控制器耦合在一起，不方便复用和测试；
4. 项目存在大量冗余导入、废弃代码，影响可读性。

## 三、解决方法与经验教训
1. 用圈复杂度工具量化评估重构前后效果，做到有理有据；
2. 严格分层：控制器只编排流程，业务实现全部下沉服务层；
3. 用抽象策略基类统一接口，新增策略只需新增类，不改动主流程；
4. 坚持小步增量重构，每次拆分后立刻测试，保证功能不变；
5. 定期清理坏味道、冗余代码，是长期维护的良好习惯。

## 四、个人总结
通过本次实验掌握了代码坏味道识别、复杂度度量、服务分层、策略模式落地与增量重构思想，理解了工业级项目高内聚低耦合、对扩展开放对修改关闭的架构设计原则。
