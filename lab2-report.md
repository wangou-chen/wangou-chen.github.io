# 软件构造实验二：ADT 与 OOP 面向对象设计心得
日期：2026-05-09

## 一、实验主要内容
本次实验围绕面向对象设计、抽象数据类型 ADT 开展实践：
1. 基于抽象基类实现新模型提供商扩展，遵守开闭原则、里氏替换原则；
2. 设计 SessionContext 自定义 ADT，封装对话上下文；
3. 定义抽象函数 AF、表示不变量 RI，实现 rep 合法性检查；
4. 采用防御性拷贝，防止外部非法篡改内部状态；
5. 编写破坏性单元测试，验证 ADT 封装安全性。

## 二、实验遇到的问题
1. 项目大量使用裸 list、dict 传递上下文，极易被外部修改污染；
2. 不理解 AF、RI 实际作用，分不清类不变量与业务约束；
3. 直接返回内部列表引用，外部可随意篡改对象状态；
4. 新增模型提供商容易写成侵入式修改，违反开闭原则。

## 三、解决方法与经验教训
1. 用 ADT 封装内部数据，只暴露有限方法，禁止外部直接访问属性；
2. 每次修改内部状态后执行 checkRep，保证始终满足表示不变量；
3. 读写都做深拷贝，采用防御性编程隔离外部引用；
4. 基于抽象基类做可插拔扩展，新增功能不修改原有核心代码；
5. 快照图、RI、AF 是保证类设计健壮性的核心手段。

## 四、个人总结
本次实验真正落地了 ADT 封装、抽象契约、多态扩展与防御性编程，理解了高内聚、低耦合的设计思想，为后续代码重构和架构设计打下基础。
